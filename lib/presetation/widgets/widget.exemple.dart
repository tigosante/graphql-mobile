import 'package:flutter/material.dart';

class WidgetExemple extends StatelessWidget {
  const WidgetExemple({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
